# AI Cover Letter Generator (FastAPI + Docker)

## Overview

This project exposes a FastAPI endpoint that uses OpenAI's GPT models
to generate a tailored cover letter based on a user's resume and a target
job description.
